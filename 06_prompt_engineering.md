# Enter within prompt

Create a retirement dashboard homepage.

Requirements:

- Read data from src/data/participants.json
- Create a Participant type
- Create a reusable ParticipantCard component
- Display all participants in a responsive grid
- Use Tailwind CSS
- Follow CLAUDE.md instructions
- Keep components under 200 lines

# Then use prompts like:

Review this project as a senior Next.js architect.
Find duplicate code and refactor it.
Add unit tests for all business logic.
Convert large components into reusable feature-based modules.
Prepare this application for production deployment.

These are the kinds of tasks where Claude Code provides the most value.

# Then follow with:

Review the code as a staff Next.js engineer.
Refactor any code smells.
Add TypeScript improvements.

This will let you experience both code generation and refactoring workflows, which are where Claude Code is most useful.

# to commit the code

commit this

# To run the app

run it
